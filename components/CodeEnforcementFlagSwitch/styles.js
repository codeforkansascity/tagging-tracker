import { StyleSheet } from 'react-native'

export default StyleSheet.create({
  codeEnforcementFlag: {
    display: 'flex',
    justifyContent: 'flex-start',
    alignItems: 'flex-start',
    flexDirection: 'row',
  },
  codeEnforcementFlagLabel: {
    top: 4,
  },
});
